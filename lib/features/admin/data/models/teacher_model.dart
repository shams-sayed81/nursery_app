class TeacherModel {
  final String id;
  final String name;
  final bool isActive;
  final int classesCount;

  TeacherModel({
    required this.id,
    required this.name,
    required this.isActive,
    required this.classesCount,
  });
}